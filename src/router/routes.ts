export interface SwitchRoutes {
  root: string;
  woman: string;
  product: string;
  cart: string;
  checkout: string;
}

export const switchRoutes: SwitchRoutes = {
  root: "/",
  woman: "/woman",
  product: "product",
  cart: "/cart",
  checkout: '/checkout'
};

