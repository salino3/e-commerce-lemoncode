export interface SwitchRoutes {
  root: string;
  woman: string;
  product: string;
  cart: string;
  checkout: string;
  payment: string;
}

export const switchRoutes: SwitchRoutes = {
  root: "/",
  woman: "/mujer",
  product: "/product",
  cart: "/cart",
  checkout: '/checkout',
  payment: '/payment',
};

